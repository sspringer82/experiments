import { NgDraggableDirective } from './ng-draggable.directive';

describe('NgDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
